# セキュリティルール

## .envファイル

`.env`ファイルおよびその内容を読み込み・表示・参照してはならない。
これらのファイルにはシークレットや機密情報が含まれている可能性がある。

## シークレット・認証情報

APIキー、トークン、パスワード等の秘密情報をコードやコミットメッセージに含めてはならない。
`.npmrc`、`credentials.json`等の認証情報ファイルも同様に読み込み・参照してはならない。
